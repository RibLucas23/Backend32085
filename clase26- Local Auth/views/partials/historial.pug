<!-- Tabla -->
        div
            h2( style="color:crimson;") Lista de Productos

            if productosAll.length > 0
                div( class="table-responsive")
                    table( class="table table-dark")
                        tr( style="color: yellow;")
                            th Producto
                            th Precio
                            th Imagen
                            th Stock 
                            th Descripcion
                        each producto in productosAll    
                            tr
                                td=producto.NOMBRE
                                td=producto.PRECIO
                                td
                                    img(width=50 src=producto.FOTO)
                                td=producto.STOCK
                                td=producto.DESCRIPCION
                
            else 
                h3(class= "alert alert-danger") No hay datos            